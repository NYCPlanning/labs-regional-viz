# Jobs
# ----------
slug: gdp-total
category: Jobs
categorySlug: jobs
title: "NYC generates half of the region’s $2 trillion in economic output."
menuTitle: "Total Gross Domestic Product"
content: |
  NEW TEXT HERE
source: |
  Sources & Notes: U.S. Bureau of Economic Analysis (BEA) Local Area Gross Domestic Product, 2021 (reported in 2020-dollars); GDP (Current $US), The World Bank [https://data.worldbank.org/](https://data.worldbank.org/).

map:
  defaultGeographyLevel: county

  sources:
  - id: gdp-total
    type: cartovector
    source-layers:
    - id: gdp-total-subregion
      sql: SELECT the_geom_webmercator, geoid, gdp_20 as value, name FROM region_subregion_v220615
    - id: gdp-total-county
      sql: SELECT the_geom_webmercator, geoid, gdp_20 as value, name FROM region_county_v220615 WHERE iscommap = 0

  popupColumns:
  - id: gdp_20
    title: '2020 GDP $billions'
    large: true
    values:
    - geomType: region
      columnName: gdp_20
      cv:
      sig:
    - geomType: subregion
      columnName: gdp_20
      cv:
      sig:
    - geomType: county
      columnName: gdp_20
      cv:
      sig:

  isPercent: false
  isDollar: true

  layerGroups:
  - id: subregion
    title: "Total Gross Domestic Product ($billions 2020-dollars)"
    legend: gdp-total-subregion
    layers:
    - id: gdp-total-subregion
      type: choropleth
      source: gdp-total
      source-layer: gdp-total-subregion
      paintConfig:
        opacity: 0.6
        colors:
        - "#ccf5df"
        - "#89dfa4"
        - "#64b678"
        - "#127723"
        - "#193f1f"
        breaks:
        - 50
        - 150
        - 300
        - 500
  - id: county
    title: "Total Gross Domestic Product ($billions 2020-dollars)"
    legend: gdp-total-county
    layers:
    - id: gdp-total-county
      type: choropleth
      source: gdp-total
      source-layer: gdp-total-county
      paintConfig:
        opacity: 0.8
        colors:
        - "#ccf5df"
        - "#89dfa4"
        - "#64b678"
        - "#127723"
        - "#193f1f"
        breaks:
        - 10
        - 25
        - 50
        - 100
