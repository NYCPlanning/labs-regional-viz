import getPopupData from 'labs-regional-viz/utils/get-popup-data';
import { module, test } from 'qunit';

module('Unit | Utility | get popup data');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPopupData();
  assert.ok(result);
});
