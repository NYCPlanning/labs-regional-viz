import getPopupSQL from 'labs-regional-viz/utils/get-popup-sql';
import { module, test } from 'qunit';

module('Unit | Utility | get popup sql');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getPopupSQL();
  assert.ok(result);
});
